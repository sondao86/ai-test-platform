# API Specification

> This file is the **contract** between Backend (Claude) and Frontend (Gemini).
> Claude writes endpoints here. Gemini reads and integrates against them.

---

## Endpoints

<!-- Example format:

### POST /api/example
**Description:** Brief description of what this endpoint does.

**Request Body:**
```json
{
  "field": "string (required) — description"
}
```

**Response (200):**
```json
{
  "id": "string",
  "status": "success"
}
```

**Error Responses:**
- `400` — Invalid request body
- `500` — Internal server error

-->

_No endpoints defined yet. Claude will populate this file when building features._
