# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/brd_pipeline

# Claude CLI (uses local `claude` command, no API key needed)
CLAUDE_MODEL=sonnet
CLAUDE_TIMEOUT=300

# App
APP_ENV=development
LOG_LEVEL=INFO
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=50
